export interface Log {
    name: string;								// 日志名称
    operator: string;							// 操作人
    operatorIp: string;							// 操作人 IP
    operation: string;							// 操作事件; 
    comment: string;                            // 备注
}
